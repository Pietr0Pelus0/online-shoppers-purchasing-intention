# online-shoppers-purchasing-intention
An end-to-end ML pipeline predicting e-commerce purchase intent. Combines unsupervised feature engineering (PCA, t-SNE, K-Means) with supervised classifiers (Random Forest, XGBoost, SVM, MLP). Handles class imbalance via SMOTE-NC and evaluates models using PR-AUC and ROC-AUC.
